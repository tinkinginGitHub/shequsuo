<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="cn.anyoufang.mapper.TbCodeMapper" >
  <resultMap id="BaseResultMap" type="cn.anyoufang.entity.TbCode" >
    <result column="code" property="code" jdbcType="VARCHAR" />
    <result column="name" property="name" jdbcType="VARCHAR" />
    <result column="valid" property="valid" jdbcType="TINYINT" />
    <result column="pcode" property="pcode" jdbcType="VARCHAR" />
    <result column="eng_name" property="engName" jdbcType="VARCHAR" />
  </resultMap>
  <sql id="Example_Where_Clause" >
    <where >
      <foreach collection="oredCriteria" item="criteria" separator="or" >
        <if test="criteria.valid" >
          <trim prefix="(" suffix=")" prefixOverrides="and" >
            <foreach collection="criteria.criteria" item="criterion" >
              <choose >
                <when test="criterion.noValue" >
                  and ${criterion.condition}
                </when>
                <when test="criterion.singleValue" >
                  and ${criterion.condition} #{criterion.value}
                </when>
                <when test="criterion.betweenValue" >
                  and ${criterion.condition} #{criterion.value} and #{criterion.secondValue}
                </when>
                <when test="criterion.listValue" >
                  and ${criterion.condition}
                  <foreach collection="criterion.value" item="listItem" open="(" close=")" separator="," >
                    #{listItem}
                  </foreach>
                </when>
              </choose>
            </foreach>
          </trim>
        </if>
      </foreach>
    </where>
  </sql>
  <sql id="Update_By_Example_Where_Clause" >
    <where >
      <foreach collection="example.oredCriteria" item="criteria" separator="or" >
        <if test="criteria.valid" >
          <trim prefix="(" suffix=")" prefixOverrides="and" >
            <foreach collection="criteria.criteria" item="criterion" >
              <choose >
                <when test="criterion.noValue" >
                  and ${criterion.condition}
                </when>
                <when test="criterion.singleValue" >
                  and ${criterion.condition} #{criterion.value}
                </when>
                <when test="criterion.betweenValue" >
                  and ${criterion.condition} #{criterion.value} and #{criterion.secondValue}
                </when>
                <when test="criterion.listValue" >
                  and ${criterion.condition}
                  <foreach collection="criterion.value" item="listItem" open="(" close=")" separator="," >
                    #{listItem}
                  </foreach>
                </when>
              </choose>
            </foreach>
          </trim>
        </if>
      </foreach>
    </where>
  </sql>
  <sql id="Base_Column_List" >
    code, name, valid, pcode, eng_name
  </sql>
  <select id="getAreas" resultType="string">
  	SELECT area FROM `tb_area_street` GROUP BY area order by convert(area using gbk) asc;
  </select>
  <select id="getStreets" resultType="string">
  	SELECT
		street
	FROM
		`tb_area_street`
	WHERE
		area = '${area}';
  </select>
  <select id="getSubway" resultType="string">
  	SELECT subway FROM `tb_subway_station` GROUP BY subway order by subway;
  </select>
  <select id="getStations" resultType="string">
  	SELECT
		station
	FROM
		`tb_subway_station`
	WHERE
		subway = '${subway}';
  </select>
  <select id="getCodeName" resultType="string" parameterType="string">
  	SELECT name FROM `tb_code` WHERE code='${code}';
  </select>
  <select id="getSubCode" resultType="map" parameterType="string">
  	SELECT code,name,eng_name FROM `tb_code` WHERE pcode='${pcode}' order by code asc;
  </select>
  <select id="getCodes" resultType="map">
  	SELECT code,name,eng_name FROM `tb_code` WHERE 1=1
  	<if test="codes!=null">
  		and code in
			<foreach collection="codes" item="item" index="index" open="(" separator="," close=")">
				'${item}'
			</foreach>
		</if>
  	order by code asc
  </select>
  <select id="selectByExample" resultMap="BaseResultMap" parameterType="cn.anyoufang.entity.TbCodeExample" >
    select
    <if test="distinct" >
      distinct
    </if>
    <include refid="Base_Column_List" />
    from tb_code
    <if test="_parameter != null" >
      <include refid="Example_Where_Clause" />
    </if>
    <if test="orderByClause != null" >
      order by ${orderByClause}
    </if>
  </select>
  <delete id="deleteByExample" parameterType="cn.anyoufang.entity.TbCodeExample" >
    delete from tb_code
    <if test="_parameter != null" >
      <include refid="Example_Where_Clause" />
    </if>
  </delete>
  <insert id="insert" parameterType="cn.anyoufang.entity.TbCode" >
    insert into tb_code (code, name, valid, 
      pcode, eng_name)
    values (#{code,jdbcType=VARCHAR}, #{name,jdbcType=VARCHAR}, #{valid,jdbcType=TINYINT}, 
      #{pcode,jdbcType=VARCHAR}, #{engName,jdbcType=VARCHAR})
  </insert>
  <insert id="insertSelective" parameterType="cn.anyoufang.entity.TbCode" >
    insert into tb_code
    <trim prefix="(" suffix=")" suffixOverrides="," >
      <if test="code != null" >
        code,
      </if>
      <if test="name != null" >
        name,
      </if>
      <if test="valid != null" >
        valid,
      </if>
      <if test="pcode != null" >
        pcode,
      </if>
      <if test="engName != null" >
        eng_name,
      </if>
    </trim>
    <trim prefix="values (" suffix=")" suffixOverrides="," >
      <if test="code != null" >
        #{code,jdbcType=VARCHAR},
      </if>
      <if test="name != null" >
        #{name,jdbcType=VARCHAR},
      </if>
      <if test="valid != null" >
        #{valid,jdbcType=TINYINT},
      </if>
      <if test="pcode != null" >
        #{pcode,jdbcType=VARCHAR},
      </if>
      <if test="engName != null" >
        #{engName,jdbcType=VARCHAR},
      </if>
    </trim>
  </insert>
  <select id="countByExample" parameterType="cn.anyoufang.entity.TbCodeExample" resultType="java.lang.Integer" >
    select count(*) from tb_code
    <if test="_parameter != null" >
      <include refid="Example_Where_Clause" />
    </if>
  </select>
  <update id="updateByExampleSelective" parameterType="map" >
    update tb_code
    <set >
      <if test="record.code != null" >
        code = #{record.code,jdbcType=VARCHAR},
      </if>
      <if test="record.name != null" >
        name = #{record.name,jdbcType=VARCHAR},
      </if>
      <if test="record.valid != null" >
        valid = #{record.valid,jdbcType=TINYINT},
      </if>
      <if test="record.pcode != null" >
        pcode = #{record.pcode,jdbcType=VARCHAR},
      </if>
      <if test="record.engName != null" >
        eng_name = #{record.engName,jdbcType=VARCHAR},
      </if>
    </set>
    <if test="_parameter != null" >
      <include refid="Update_By_Example_Where_Clause" />
    </if>
  </update>
  <update id="updateByExample" parameterType="map" >
    update tb_code
    set code = #{record.code,jdbcType=VARCHAR},
      name = #{record.name,jdbcType=VARCHAR},
      valid = #{record.valid,jdbcType=TINYINT},
      pcode = #{record.pcode,jdbcType=VARCHAR},
      eng_name = #{record.engName,jdbcType=VARCHAR}
    <if test="_parameter != null" >
      <include refid="Update_By_Example_Where_Clause" />
    </if>
  </update>
</mapper>